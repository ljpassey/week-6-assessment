const {shuffleArray} = require('./utils')

describe('shuffleArray should', () => {
    test('type of shuffleArray should be array', () => {
        expect(typeof shuffleArray()).toBe('array')
    })
    test('returns the same length as the arguments sent in', () => {
        
    })
   
})

